services:
  frontend:
    build: 
      context: ./audio-intel-ui
      dockerfile: Dockerfile
    ports:
      - "5173:5173"

    volumes:
      - ./audio-intel-ui:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development