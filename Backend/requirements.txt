fastapi
uvicorn[standard]
python-multipart
platformdirs
httpx
numpy

# NLP (Ofir — coded-language detection)
transformers           # distilgpt2 for Signal C (perplexity)
torch                  # backend for transformers + sentence-transformers
sentence-transformers  # BAAI/bge-small-en-v1.5 segment embeddings (shared infra; Hadar will reuse)
scikit-learn           # TfidfVectorizer + CountVectorizer + cosine
faiss-cpu              # dense vector index for semantic search (Hadar)
rank_bm25              # BM25Okapi lexical retrieval (Hadar)

# NLP (Ofek — NER + Entity Resolution)
phonetics              # Double Metaphone for phonetic name matching
jellyfish              # Jaro-Winkler string similarity
