# Copy to .env and set to your central server's IP/hostname.
# When empty, falls back to localhost (for local dev).
VITE_API_BASE_URL=http://YOUR_SERVER_IP:8001
VITE_ML_BASE_URL=http://YOUR_SERVER_IP:8000
